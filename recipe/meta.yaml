{% set name = "fairchem-core" %}
{% set version = "2.11.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/fairchem_core-{{ version }}.tar.gz
  sha256: 7405a43a8dc743d9d5e642be9cee8b9583795bd62298498c59a768ff442f3883

build:
  entry_points:
    - fairchem = fairchem.core._cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.10,<3.14
    - hatchling
    - hatch-vcs
    - hatch-fancy-pypi-readme >=24
    - pip
  run:
    - python >=3.10,<3.14
    - pytorch >=2.8.0,<2.9.dev0
    - numpy >=2.0,<2.3
    - lmdb ==1.6.2
    - numba >=0.61.2
    - e3nn >=0.5
    - huggingface_hub >=0.27.1
    - ase >=3.26.0
    - ase-db-backends >=0.10.0
    - monty >=v2025.1.3
    - clusterscope ==0.0.18
    - requests
    - orjson
    - tqdm
    - submitit
    - hydra-core
    - torchtnt
    - pyyaml
    - wandb
    - websockets

test:
  requires:
    - python {{ python_min }}
  imports:
    - fairchem.core
  commands:
    - fairchem --help

about:
  home: https://github.com/FAIR-Chem/fairchem
  summary: Machine learning models for use in catalysis as part of the Open Catalyst Project
  license: MIT
  license_file: src/fairchem/core/LICENSE.md

extra:
  recipe-maintainers:
    - jan-janssen
